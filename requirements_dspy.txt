# DSPy dependencies
dspy-ai>=2.3.9
torch>=2.1.0
distro>=1.9.0
backoff>=2.2.1

# Core dependencies (already in main requirements.txt but listed here for completeness)
openai>=1.3.0
langchain>=0.1.0
streamlit>=1.24.0
python-dotenv>=1.0.0 